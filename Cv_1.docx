Čítače
Dopředný čítač (CTU)
Čítač nahoru (CTU - Count Up Counter) je typ čítače v programovatelných logických automatech (PLC), který se používá k inkrementaci hodnoty při každém aktivaci vstupního signálu. Tento čítač zvyšuje svoji aktuální hodnotu (CV - Current Value) o jeden při každém pozitivním okraji signálu na vstupu CU (Count Up).

(* Deklarace proměnných *)
VAR
    CTU_Example : CTU; (* Čítač nahoru *)
    Increment : BOOL; (* Vstupní signál pro inkrementaci *)
    Reset : BOOL;     (* Vstupní signál pro resetování čítače *)
    Count_Up_Value : INT := 10; (* Přednastavená hodnota pro čítač nahoru *)
    Up_Count : INT;   (* Aktuální hodnota čítače nahoru *)
END_VAR

(* Čítač nahoru (CTU) - Inkrementace *)
CTU_Example(CU := Increment, R := Reset, PV := Count_Up_Value);

(* Zpracování výstupních signálů a aktuální hodnoty čítače *)
Up_Count := CTU_Example.CV; (* Aktuální hodnota čítače nahoru *)

IF CTU_Example.Q THEN
    (* Akce při dosažení přednastavené hodnoty čítače nahoru *)
    (* Například: *)
    (* Output := TRUE; *)
END_IF
